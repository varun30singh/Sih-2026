export class Farmer {
  id: string;
  userId: string;
  name: string;
  idProof: string;
  village: string;
  phone: string;
  stockistId: string;
  createdAt: Date;
}

// Database column mapping (snake_case):
// id, user_id, name, id_proof, village, phone, stockist_id, created_at
